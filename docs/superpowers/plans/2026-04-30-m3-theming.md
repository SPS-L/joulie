# M3 Theming Refactor — Implementation Plan

> **For agentic workers:** Tasks use checkbox (`- [ ]`) syntax. Spec lives at `docs/superpowers/specs/2026-04-30-m3-theming-design.md`.

**Goal:** Land a fully Material 3 token system on `chore/m3-theming` — full light + dark palettes seeded from `#1565C0` (tertiary overridden to an orange ramp anchored on `#FB8C00`), full M3 role map in `themes.xml`, no layout edits, no code changes.

**Branch:** `chore/m3-theming` (already created and checked out; spec committed as `a1a9871`).

**Sandbox build commands** (CLAUDE.md note — `~/.gradle` is read-only here):

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebug
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:testDebugUnitTest
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebugAndroidTest
```

CLAUDE.md global rules: never compound git commands with `&&`; run each separately.

---

## File touches

| File | Change |
|---|---|
| `app/src/main/res/values-night/colors.xml` | **Create** — full dark M3 palette |
| `app/src/main/res/values/colors.xml` | **Replace** — M2 names dropped, full M3 light palette + `chart_*` palette + `chart_*_fallback` kept |
| `app/src/main/res/values/themes.xml` | **Rewrite** — 30 M3 role mappings; `colorPrimaryVariant` / `colorSecondaryVariant` removed |
| `CLAUDE.md` | **Update** — Status block notes M3 theming refactor merged (Task 5) |

No layout edits. No Kotlin edits. No new tests (theming is XML-only; JVM tests don't exercise theme attrs).

---

## Task 1: Create dark palette `values-night/colors.xml`

**Files:**
- Create: `app/src/main/res/values-night/colors.xml`

- [ ] **Step 1: Create the file**

Write `app/src/main/res/values-night/colors.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- M3 — Primary -->
    <color name="md_theme_light_primary">#9ECAFF</color>
    <color name="md_theme_light_onPrimary">#003258</color>
    <color name="md_theme_light_primaryContainer">#00497D</color>
    <color name="md_theme_light_onPrimaryContainer">#D1E4FF</color>

    <!-- M3 — Secondary -->
    <color name="md_theme_light_secondary">#BBC7DB</color>
    <color name="md_theme_light_onSecondary">#253140</color>
    <color name="md_theme_light_secondaryContainer">#3B4858</color>
    <color name="md_theme_light_onSecondaryContainer">#D7E3F8</color>

    <!-- M3 — Tertiary (orange ramp inverted from light) -->
    <color name="md_theme_light_tertiary">#FFB68D</color>
    <color name="md_theme_light_onTertiary">#552100</color>
    <color name="md_theme_light_tertiaryContainer">#793300</color>
    <color name="md_theme_light_onTertiaryContainer">#FFDCC0</color>

    <!-- M3 — Error -->
    <color name="md_theme_light_error">#FFB4AB</color>
    <color name="md_theme_light_onError">#690005</color>
    <color name="md_theme_light_errorContainer">#93000A</color>
    <color name="md_theme_light_onErrorContainer">#FFDAD6</color>

    <!-- M3 — Neutral / surface -->
    <color name="md_theme_light_background">#1A1C1E</color>
    <color name="md_theme_light_onBackground">#E2E2E6</color>
    <color name="md_theme_light_surface">#1A1C1E</color>
    <color name="md_theme_light_onSurface">#E2E2E6</color>
    <color name="md_theme_light_surfaceVariant">#43474E</color>
    <color name="md_theme_light_onSurfaceVariant">#C3C7CF</color>
    <color name="md_theme_light_outline">#8D9199</color>
    <color name="md_theme_light_outlineVariant">#43474E</color>

    <!-- M3 — Inverse + scrim -->
    <color name="md_theme_light_inverseSurface">#E2E2E6</color>
    <color name="md_theme_light_inverseOnSurface">#2F3033</color>
    <color name="md_theme_light_inversePrimary">#0061A4</color>
    <color name="md_theme_light_scrim">#000000</color>
</resources>
```

> **Naming.** Resource ids use `md_theme_light_*` for both light and dark files — the `_light_` is fixed identifier text, not a mode marker. Android selects the correct file based on `values/` vs `values-night/` qualifier; the same `themes.xml` mapping covers both modes.

- [ ] **Step 2: Build to confirm the file is valid XML and the resource ids parse**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebug
```

Expected: BUILD SUCCESSFUL. (At this point the new resource ids exist alongside the old ones; nothing references the new ids yet, but the build still must pass.)

- [ ] **Step 3: Commit**

```bash
git add app/src/main/res/values-night/colors.xml
git commit -m "feat(theme): add values-night/colors.xml with full M3 dark palette"
```

---

## Task 2: Replace `values/colors.xml`

**Files:**
- Modify: `app/src/main/res/values/colors.xml` (full replacement)

- [ ] **Step 1: Replace the file**

Overwrite `app/src/main/res/values/colors.xml` with:

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- M3 — Primary (seeded #1565C0) -->
    <color name="md_theme_light_primary">#0061A4</color>
    <color name="md_theme_light_onPrimary">#FFFFFF</color>
    <color name="md_theme_light_primaryContainer">#D1E4FF</color>
    <color name="md_theme_light_onPrimaryContainer">#001D36</color>

    <!-- M3 — Secondary (algorithmic, seeded #1565C0) -->
    <color name="md_theme_light_secondary">#535F70</color>
    <color name="md_theme_light_onSecondary">#FFFFFF</color>
    <color name="md_theme_light_secondaryContainer">#D7E3F8</color>
    <color name="md_theme_light_onSecondaryContainer">#101C2B</color>

    <!-- M3 — Tertiary (overridden orange ramp anchored on #FB8C00 — DESIGN §6 "DC orange") -->
    <color name="md_theme_light_tertiary">#A04E00</color>
    <color name="md_theme_light_onTertiary">#FFFFFF</color>
    <color name="md_theme_light_tertiaryContainer">#FFDCC0</color>
    <color name="md_theme_light_onTertiaryContainer">#341000</color>

    <!-- M3 — Error -->
    <color name="md_theme_light_error">#BA1A1A</color>
    <color name="md_theme_light_onError">#FFFFFF</color>
    <color name="md_theme_light_errorContainer">#FFDAD6</color>
    <color name="md_theme_light_onErrorContainer">#410002</color>

    <!-- M3 — Neutral / surface -->
    <color name="md_theme_light_background">#FDFCFF</color>
    <color name="md_theme_light_onBackground">#1A1C1E</color>
    <color name="md_theme_light_surface">#FDFCFF</color>
    <color name="md_theme_light_onSurface">#1A1C1E</color>
    <color name="md_theme_light_surfaceVariant">#DFE2EB</color>
    <color name="md_theme_light_onSurfaceVariant">#43474E</color>
    <color name="md_theme_light_outline">#73777F</color>
    <color name="md_theme_light_outlineVariant">#C3C7CF</color>

    <!-- M3 — Inverse + scrim -->
    <color name="md_theme_light_inverseSurface">#2F3033</color>
    <color name="md_theme_light_inverseOnSurface">#F1F0F4</color>
    <color name="md_theme_light_inversePrimary">#9ECAFF</color>
    <color name="md_theme_light_scrim">#000000</color>

    <!-- F2 — Chart palette (kept verbatim from F2; non-theme product colors) -->
    <color name="chart_1">#1565C0</color>
    <color name="chart_2">#00796B</color>
    <color name="chart_3">#F57F17</color>
    <color name="chart_4">#AD1457</color>
    <color name="chart_5">#6A1B9A</color>

    <!-- F2 — Chart series fallback colors. Used only when theme attrs
         (?attr/colorPrimary, ?attr/colorTertiary) cannot be resolved. With the
         M3 theme rewrite the theme attrs *do* resolve, but the fallbacks remain
         for defensive code paths. -->
    <color name="chart_ac_fallback">#1E88E5</color>
    <color name="chart_dc_fallback">#FB8C00</color>
</resources>
```

> The old M2 names (`primary`, `primary_variant`, `secondary`, `secondary_variant`, `background`, `surface`, `error`, `on_primary`, `on_secondary`, `on_background`, `on_surface`) are **dropped**. Existing `themes.xml` still references them at this point — Task 3 fixes that. So this commit alone temporarily breaks the build; we accept that because Task 3 lands seconds later.
>
> **Alternative if you prefer always-green commits:** swap the order of Tasks 2 and 3, OR squash Tasks 2+3 into one commit. The trade-off: separate commits give cleaner blame for the colour-resource swap vs. the theme-attribute mapping. I'll keep them separate; the mid-stream broken state is acceptable on a feature branch never merged in this state.

- [ ] **Step 2: Verify the build is INTENTIONALLY broken**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebug
```

Expected: FAIL with `Resource not found: @color/primary` (or similar — `themes.xml` still references the dropped names). This is the expected mid-stream state.

- [ ] **Step 3: Stage but do NOT commit yet**

```bash
git add app/src/main/res/values/colors.xml
```

Do not commit until Task 3's `themes.xml` is also written, then commit both together. (See Task 3 Step 4.)

---

## Task 3: Rewrite `values/themes.xml`

**Files:**
- Modify: `app/src/main/res/values/themes.xml` (full rewrite)

- [ ] **Step 1: Replace the file**

Overwrite `app/src/main/res/values/themes.xml` with:

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EVTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_light_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_light_onPrimaryContainer</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/md_theme_light_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_light_onSecondary</item>
        <item name="colorSecondaryContainer">@color/md_theme_light_secondaryContainer</item>
        <item name="colorOnSecondaryContainer">@color/md_theme_light_onSecondaryContainer</item>

        <!-- Tertiary (orange) -->
        <item name="colorTertiary">@color/md_theme_light_tertiary</item>
        <item name="colorOnTertiary">@color/md_theme_light_onTertiary</item>
        <item name="colorTertiaryContainer">@color/md_theme_light_tertiaryContainer</item>
        <item name="colorOnTertiaryContainer">@color/md_theme_light_onTertiaryContainer</item>

        <!-- Error -->
        <item name="colorError">@color/md_theme_light_error</item>
        <item name="colorOnError">@color/md_theme_light_onError</item>
        <item name="colorErrorContainer">@color/md_theme_light_errorContainer</item>
        <item name="colorOnErrorContainer">@color/md_theme_light_onErrorContainer</item>

        <!-- Background / surface -->
        <item name="android:colorBackground">@color/md_theme_light_background</item>
        <item name="colorOnBackground">@color/md_theme_light_onBackground</item>
        <item name="colorSurface">@color/md_theme_light_surface</item>
        <item name="colorOnSurface">@color/md_theme_light_onSurface</item>
        <item name="colorSurfaceVariant">@color/md_theme_light_surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/md_theme_light_onSurfaceVariant</item>
        <item name="colorOutline">@color/md_theme_light_outline</item>
        <item name="colorOutlineVariant">@color/md_theme_light_outlineVariant</item>

        <!-- Inverse + scrim -->
        <item name="colorSurfaceInverse">@color/md_theme_light_inverseSurface</item>
        <item name="colorOnSurfaceInverse">@color/md_theme_light_inverseOnSurface</item>
        <item name="colorPrimaryInverse">@color/md_theme_light_inversePrimary</item>
        <item name="scrimBackground">@color/md_theme_light_scrim</item>
    </style>

    <style name="Theme.EVTracker.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:colorBackground</item>
        <item name="postSplashScreenTheme">@style/Theme.EVTracker</item>
    </style>
</resources>
```

- [ ] **Step 2: Build**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebug
```

Expected: BUILD SUCCESSFUL. All M3 colour-role attrs now resolve via the new `md_theme_light_*` resource ids.

- [ ] **Step 3: Run JVM tests to confirm zero regression**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:testDebugUnitTest
```

Expected: BUILD SUCCESSFUL. (No tests touch theming, but verify the suite still compiles and passes.)

- [ ] **Step 4: Verify no stale M2 colour references**

```bash
git grep -E '@color/(primary|primary_variant|secondary|secondary_variant|background|surface|error|on_primary|on_secondary|on_background|on_surface)\b' app/src/main/
```

Expected: zero matches.

```bash
git grep -E 'colorPrimaryVariant|colorSecondaryVariant' app/src/main/
```

Expected: zero matches.

- [ ] **Step 5: Commit Tasks 2 + 3 together (single green commit)**

```bash
git add app/src/main/res/values/themes.xml
git commit -m "feat(theme): full M3 token map — drop M2 attrs, swap colors.xml to md_theme_light_*"
```

(`values/colors.xml` was staged in Task 2 Step 3 and is included in this commit.)

---

## Task 4: Build verification across all targets

**Files:** none modified.

- [ ] **Step 1: Assemble debug**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebug
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 2: Compile instrumented test source set**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:assembleDebugAndroidTest
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Run JVM unit tests (full suite)**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:testDebugUnitTest
```

Expected: BUILD SUCCESSFUL; ~236 tests still pass (no theming changes touch the JVM-side paths).

No commit in this task — verification only.

---

## Task 5: CLAUDE.md status bump + ship

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Update the Status block**

In `CLAUDE.md`, replace the existing `> **Status:** ...` line with:

```
> **Status:** Sub-projects A (foundation/DI/Room v3), B (repositories), C (domain services + use cases), D (Core UI: Dashboard/ChargeEdit/Cars/History), E (Drive backup), F1 (Settings remainder + ManageLocations + reset use cases + startup auto-recovery), F2 (Charts), and the M3 theming refactor are all merged. Wizard, Dashboard, ChargeEdit, Cars, History, Settings, ManageLocations, and Charts are fully wired with a complete Material 3 token system (light + dark palettes seeded from #1565C0; tertiary overridden to a #FB8C00 orange ramp for DESIGN §6 "DC orange"). JVM unit-test count: ~236. Instrumented suite compiles via `:app:assembleDebugAndroidTest` (running requires an emulator); Drive backup smoke per `GOOGLE_CLOUD_SETUP.md` requires a Google account allow-listed in the OAuth consent screen.
```

- [ ] **Step 2: Commit the status bump**

```bash
git add CLAUDE.md
git commit -m "docs(theme): note M3 theming refactor in CLAUDE.md status"
```

- [ ] **Step 3: Merge to main**

Run each git command separately (CLAUDE.md global rule):

```bash
git checkout main
```

```bash
git pull --ff-only origin main
```

```bash
git merge --no-ff chore/m3-theming -m "Merge M3 theming refactor into main"
```

- [ ] **Step 4: Post-merge sanity**

```bash
GRADLE_USER_HOME=/tmp/gradle-home ./gradlew :app:testDebugUnitTest
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 5: Push and delete branch**

```bash
git push origin main
```

```bash
git branch -d chore/m3-theming
```

(`Device or resource busy` warning on `git branch -d` is the OneDrive-on-Linux quirk we've seen before — branch deletion still completes.)

---

## Self-review notes

**Spec coverage:** every section of `2026-04-30-m3-theming-design.md` is mapped:

| Spec § | Task |
|---|---|
| §2.1 light palette | Task 2 |
| §2.2 dark palette | Task 1 |
| §2.3 removed M2 tokens | Task 2 (drop) + Task 3 verify-step (grep) |
| §3 theme rewrite | Task 3 |
| §4 ChartStyling unchanged | Asserted by Task 4 build (no code edits) |
| §5 acceptance criteria — grep + builds | Task 3 Step 4 + Task 4 |
| §5 acceptance criteria — manual smoke | Out-of-band, post-merge; document in merge commit |
| §6 deferrals (DynamicColors, MPAndroidChart axis text) | None of these are in the plan — confirmed deferred |
| §7 file touches | Tasks 1, 2, 3, 5 |

**Placeholder scan:** clean — no "TBD"/"TODO"/"fill in" anywhere.

**Type/name consistency:** the resource ids `md_theme_light_*` are used identically across `values/colors.xml`, `values-night/colors.xml`, and the `@color/...` references in `values/themes.xml` (Task 3 grep would catch any drift). Acceptance §5 also greps for the dropped M2 names.

**Manual smoke** per §5 of the spec is an explicit post-merge step; no automation in this plan.
